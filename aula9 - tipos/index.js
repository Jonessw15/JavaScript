// String, number, undefined, null, boolean e symbol
const nome = 'Jones' // String
const num = 10 // Number
let nomeAluno // undefined = não aponta para nenhuma local, ou seja, fica esperando receber o valor da variável
const sobrenomeAluno = null // Valor -> Nulo
const aprovado = true // Boolean = true & false (dados lógicos)

console.log(typeof aprovado, aprovado)
